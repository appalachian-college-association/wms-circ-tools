# =============================================================================
# OCLC SFTP Connection Settings
# =============================================================================
HOST_NAME=sftp.oclc.org
HOST_PORT=22

# Server fingerprint for security verification
# Get this by running: python data_fetcher.py --print-fingerprint
FINGERPRINT=SHA256:your_verified_fingerprint_here


# =============================================================================
# Library SFTP Credentials
# =============================================================================
# Format: {FULL_LIB_CODE}_USER and {FULL_LIB_CODE}_PASS
# Note: Use the COMPLETE lib_code including any prefix (e.g., WX_)
#
# Example for library code 'wx_abc':
WX_ABC_USER=your_oclc_username
WX_ABC_PASS=your_oclc_password


# =============================================================================
# Institution IDs (Registry IDs)
# =============================================================================
# Format: {SYMBOL}_INSTITUTION_ID (OCLC symbol only, no prefix!)
# Note: Use ONLY the OCLC symbol, NOT the full lib_code
#
# Find your institution ID in OCLC WMS: Collection Manager → Institution Settings
#
# Example library code 'wx_abc' (symbol: ABC):
ABC_INSTITUTION_ID=1234


# =============================================================================
# Optional Settings
# =============================================================================

# Expiration date for patron reloads (YYYY-MM-DD format)
# Apply --expiration-date CLI flag to use this date
EXPIRATION_DATE=2026-12-31

# Barcode validation limits (optional overrides)
SOFT_MAX_BARCODE_LEN=20
HARD_MAX_BARCODE_BYTES=30